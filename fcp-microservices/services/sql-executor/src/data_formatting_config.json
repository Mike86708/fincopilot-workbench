{
    "formatting_groups": [
        {
            "name": "Standard Formatting",
            "instructions": [
                { "type": "format_date", "column_patterns": ["*DATE*","*MONTH*"], "format":"%m/%d/%Y"},
                { "type": "add_thousand_separator", "column_patterns": ["*AMOUNT*", "*TOTAL*","*BALANCE*"] },
                { "type": "remove_columns", "column_patterns": ["*_PK","*_FK"] }
            ]
        },
        {
            "name": "Custom Formatting",
            "instructions": [
                { "type": "format_date", "column_patterns": ["*DATE*","*MONTH*"], "format":"%m/%d/%Y"},
                { "type": "remove_columns", "column_patterns": ["*_PK","*_FK"] }
            ]
        },
        {
            "name": "No Formatting",
            "instructions": []
        }
    ]
}
